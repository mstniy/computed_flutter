name: computed_flutter
description: Computed for Flutter

publish_to: "none"
version: 0.0.0+0

environment:
  sdk: ">=2.17.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  built_collection: ^5.1.1
  computed:
    path: ../computed
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
